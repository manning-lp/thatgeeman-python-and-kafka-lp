[tool.poetry]
name = "pg-data"
version = "0.1.0"
description = ""
authors = ["Rob Koch <rob.koch@live.com>"]

[tool.poetry.dependencies]
python = "^3.8"
psycopg2 = "^2.9.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
